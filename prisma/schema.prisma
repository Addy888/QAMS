// This root-level schema.prisma is intentionally empty.
// The real Prisma schema and all models live in apps/api/prisma/schema.prisma.
// Do NOT add models or a generator here — doing so causes pnpm to hoist an
// empty @prisma/client to the workspace root and shadow the API's generated client.
